import React from 'react';
import { Typography, Container } from '@mui/material';

const Home = () => (
  <Container>
    <Typography variant="h4">Welcome to DFNDS</Typography>
    <Typography>Navigate to /check to verify articles.</Typography>
  </Container>
);

export default Home;
